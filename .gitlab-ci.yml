stages:
  - build
  - build-image

image: maven:3.9.2-eclipse-temurin-17

build-job:
  stage: build
  tags: [linux, shell]
  script:
    - mvn clean verify

build-image:
  stage: build-image
  tags: [linux, shell]
  script:
    - mvn clean spring-boot:build-image